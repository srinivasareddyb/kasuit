_schema-version: "3.2"
ID: comscmscm
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comscmscm-destination-content
  type: com.sap.application.content
  requires:
  - name: comscmscm-destination-service
    parameters:
      content-target: true
  - name: comscmscm-repo-host
    parameters:
      service-key:
        name: comscmscm-repo-host-key
  - name: comscmscm-uaa
    parameters:
      service-key:
        name: comscmscm-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comscmscm_html_repo_host
          ServiceInstanceName: comscmscm-html5-srv
          ServiceKeyName: comscmscm-repo-host-key
          sap.cloud.service: comscmscm
        - Authentication: OAuth2UserTokenExchange
          Name: comscmscm_uaa
          ServiceInstanceName: comscmscm-xsuaa-srv
          ServiceKeyName: comscmscm-uaa-key
          sap.cloud.service: comscmscm
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comscmscm-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comscmscm-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comscmscm.zip
      name: comscmscm
      target-path: resources/
- name: comscmscm
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comscmscm-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comscmscm-destination-service
    service-plan: lite
- name: comscmscm-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comscmscm-xsuaa-srv
    service-plan: application
- name: comscmscm-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comscmscm-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
